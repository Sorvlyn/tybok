"""CLI checks.

``surface``   the command line of ``python -m tybok`` itself: every subcommand's options, defaults
              and dispatch against ``tests/cli/expected.json``, the parent -> child flag wiring,
              and a smoke run (five jobs; see ``surface.py`` for what each one pins)
"""
